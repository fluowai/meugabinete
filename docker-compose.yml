name: political-intelligence-os

services:
  traefik:
    image: traefik:v2.11
    restart: unless-stopped
    depends_on:
      frontend:
        condition: service_healthy
      api:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic:/traefik/dynamic:ro
      - traefik_acme:/traefik
    networks:
      - pios
    cpus: "0.50"
    mem_limit: 256m
    logging: &default-logging
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
        VITE_PANEL_URL: ${APP_URL:-https://app.political-os.com.br}
    env_file:
      - .env.production
    environment:
      VITE_API_URL: same-origin
      VITE_WHATSAPP_API_URL: /api/whatsapp
      VITE_WHATSAPP_WS_URL: /api/whatsapp/ws
      VITE_PUBLIC_APP_URL: ${PUBLIC_APP_URL:-https://political-os.com.br}
      VITE_PANEL_URL: ${APP_URL:-https://app.political-os.com.br}
      VITE_PLATFORM_IP: ${PLATFORM_PUBLIC_IP:-207.58.153.219}
      VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
      VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
    expose:
      - "80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - pios
    cpus: "0.50"
    mem_limit: 256m
    logging: *default-logging
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3002
      TRAEFIK_DYNAMIC_DIR: /traefik/dynamic
      WHATSAPP_API_URL: http://whatsapp-service:3100
      WHATSMEOW_URL: http://whatsapp-service:3100
      WHATSAPP_SERVICE_TOKEN: ${WHATSAPP_SERVICE_TOKEN:?set WHATSAPP_SERVICE_TOKEN}
      WHATSAPP_INTERNAL_TOKEN: ${WHATSAPP_INTERNAL_TOKEN:?set WHATSAPP_INTERNAL_TOKEN}
      WHATSAPP_WS_JWT_SECRET: ${WHATSAPP_WS_JWT_SECRET:?set WHATSAPP_WS_JWT_SECRET}
      NODE_URL: http://api:3002
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-https://app.political-os.com.br,https://political-os.com.br}
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.political-os.com.br,https://political-os.com.br}
    volumes:
      - ./traefik/dynamic:/traefik/dynamic
    expose:
      - "3002"
    depends_on:
      whatsapp-service:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3002/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - pios
    cpus: "1.00"
    mem_limit: 768m
    logging: *default-logging
    restart: unless-stopped

  whatsapp-service:
    build:
      context: .
      dockerfile: Dockerfile.whatsapp
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3100
      NODE_URL: http://api:3002
      WHATSMEOW_URL: http://whatsapp-service:3100
      WHATSAPP_API_URL: http://whatsapp-service:3100
      WHATSAPP_SERVICE_TOKEN: ${WHATSAPP_SERVICE_TOKEN:?set WHATSAPP_SERVICE_TOKEN}
      WHATSAPP_INTERNAL_TOKEN: ${WHATSAPP_INTERNAL_TOKEN:?set WHATSAPP_INTERNAL_TOKEN}
      PGSSLMODE: require
      CORS_ORIGINS: ${CORS_ORIGINS:-https://app.political-os.com.br,https://political-os.com.br}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-https://app.political-os.com.br,https://political-os.com.br}
    volumes:
      - whatsapp_sessions:/app/.sessions
    expose:
      - "3100"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:3100/health >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - pios
    cpus: "1.50"
    mem_limit: 1g
    logging: *default-logging
    restart: unless-stopped

volumes:
  whatsapp_sessions:
  traefik_acme:

networks:
  pios:
    driver: bridge
