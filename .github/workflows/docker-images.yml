name: Docker Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: frontend
            image: ghcr.io/fluowai/pios-frontend
            dockerfile: Dockerfile.frontend
            deployment_alias: ghcr.io/fluowai/pios-frontend:latest
          - name: api
            image: ghcr.io/fluowai/pios-api
            dockerfile: Dockerfile.api
            deployment_alias: ghcr.io/fluowai/pios-api:latest
          - name: whatsapp
            image: ghcr.io/fluowai/pios-whatsapp
            dockerfile: Dockerfile.whatsapp
            deployment_alias: ghcr.io/fluowai/pios-whatsapp:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ matrix.image }}:latest
            ${{ matrix.image }}:${{ github.sha }}
            ${{ matrix.deployment_alias }}
          build-args: |
            VITE_API_URL=same-origin
            VITE_WHATSAPP_API_URL=/api/whatsapp
            VITE_WHATSAPP_WS_URL=/api/whatsapp/ws
            VITE_SUPABASE_URL=runtime
            VITE_SUPABASE_ANON_KEY=runtime
            VITE_PANEL_URL=https://app.political-os.com.br

  deploy-portainer:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Redeploy Portainer stack
        id: redeploy
        env:
          PORTAINER_WEBHOOK_URL: ${{ secrets.PORTAINER_WEBHOOK_URL }}
        run: |
          if [ -z "$PORTAINER_WEBHOOK_URL" ]; then
            echo "PORTAINER_WEBHOOK_URL is not configured. Images were published, but Portainer redeploy was skipped."
            echo "triggered=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          curl --fail --show-error --silent \
            --request POST \
            "$PORTAINER_WEBHOOK_URL"
          echo "triggered=true" >> "$GITHUB_OUTPUT"

      - name: Verify production API routes
        if: steps.redeploy.outputs.triggered == 'true'
        env:
          PRODUCTION_API_URL: ${{ vars.PRODUCTION_API_URL || 'https://political-os.com.br' }}
          PRODUCTION_APP_URL: ${{ vars.PRODUCTION_APP_URL || 'https://app.political-os.com.br' }}
        run: |
          for attempt in $(seq 1 12); do
            system_status=$(curl --silent --output /dev/null --write-out "%{http_code}" \
              "$PRODUCTION_API_URL/api/system-status")
            runtime_config=$(curl --fail --silent --show-error \
              -H "Cache-Control: no-cache" \
              "$PRODUCTION_APP_URL/env-config.js" || true)
            supabase_url=$(printf '%s' "$runtime_config" | sed -n 's/.*VITE_SUPABASE_URL: "\([^"]*\)".*/\1/p')
            supabase_key=$(printf '%s' "$runtime_config" | sed -n 's/.*VITE_SUPABASE_ANON_KEY: "\([^"]*\)".*/\1/p')
            supabase_status="000"
            if [ -n "$supabase_url" ] && [ -n "$supabase_key" ]; then
              supabase_status=$(curl --silent --output /dev/null --write-out "%{http_code}" \
                -H "apikey: $supabase_key" \
                -H "Authorization: Bearer $supabase_key" \
                "$supabase_url/rest/v1/site_texts?select=key&limit=1" || true)
            fi

            if [ "$system_status" = "200" ] && [ "$supabase_status" = "200" ]; then
              echo "Production API and public Supabase access are healthy."
              exit 0
            fi

            echo "Attempt $attempt/12: system-status=$system_status supabase=$supabase_status"
            sleep 10
          done

          echo "Production API routes or public Supabase credentials are invalid."
          exit 1
